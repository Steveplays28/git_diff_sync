name: Continuous integration
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: |
          rustup toolchain install
          apt-get update
          apt-get -y --no-install-recommends install mold clang
          cargo test --all

  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: |
          rustup toolchain install
          apt-get update
          apt-get -y --no-install-recommends install mold clang
          cargo fmt --all -- --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: |
          rustup toolchain install
          apt-get update
          apt-get -y --no-install-recommends install mold clang
          cargo clippy --all -- -D warnings
